# tools/ 개발 전용 의존성 — 배포용 requirements.txt에는 포함하지 않는다
pandas>=2.0.0
scikit-learn>=1.3.0
pytest>=7.4.0

# ── Phase 2: PyTorch 학습 (tools/train_torch.py) ──
# torch는 CPU 전용 휠을 받는다 — 이 프로젝트 데이터 규모엔 GPU가 불필요하고, CPU 휠이 훨씬 작다
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
# [중요] onnx 1.17+ 는 protobuf 4 이상을 끌어와 mediapipe 0.10.9(protobuf<4 필요)를 깨뜨린다.
#        배포 requirements.txt 와 같은 protobuf<4 핀을 여기서도 유지한다.
onnx>=1.15.0,<1.17
protobuf<4
onnxruntime>=1.17.0
matplotlib>=3.8.0
